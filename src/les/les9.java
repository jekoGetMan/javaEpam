package les;

public class les9 {
 // checked искепшн нужно что-то в try, какой-то опасный код, иначе подчеркнет, типа что catch справшивает, что мне чекать?
 // разница try/catch
 // 1 возращается когда exception
 // final выполнится по любому
 // При рекурсии будет stackoverflow
 // finalizze вызывает garbage collector
 // BufferReader - вводим с клавы
 // Многопоточность
 // сначала более узкие, а потом более широкие (про exception) exception должно быть внизу
 // он может быть шире но не уже (модификаторы)
    // try with rousrses / multi except // java 1.7
    //  10 / 0 - unchecked (компилятор не будет ругаться)
    // 10 / 0 - arithmetic
    // 10.0 / 0.0 - infinity
    // unchecked - error - и все его наследники
    // checked - Exception и все его наследники
    // NPE - nullPointerException
    // entity сущность
    // паттерн MVC (model view controler)
    // в мейн создание объектов и вызов методов
    // имя класса + exception
    // пробрасываем икспешн через объект
    // сhecked exception нужно обрабатывать через try/catch
    // checked exception не всегда нужны
    // Переходы java 6/7/8
    // E -elem / T -type / V -value / K -key
    // сырой тип(коллекции)
    // instanceof ( как вытащить из сырой коллекции тип integer)
    // getClass
    // Collection[s] - если s, то все классы статические
    // При сравнении - отнимаем
    // сортировка в значению(массивы/объекты)
    // ArrayList / Hashmap
    // алгоритмическая сложность (где долго, где быстро)
    //




}
